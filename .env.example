# Optional — enables Claude-generated summaries. Without it, the platform
# falls back to clean extractive summaries built from feed excerpts.
ANTHROPIC_API_KEY=

# Optional overrides
# SUMMARY_MODEL=claude-opus-4-8
# PORT=3000
# REFRESH_INTERVAL_MS=600000
